export const domElements = {
  myPlantsBtn: { classList: { add: jest.fn(), remove: jest.fn(), contains: jest.fn() } },
  addNewPlantBtn: { classList: { add: jest.fn(), remove: jest.fn(), contains: jest.fn() } },
  quizBtn: { classList: { add: jest.fn(), remove: jest.fn(), contains: jest.fn() } },
  libraryBtn: { classList: { add: jest.fn(), remove: jest.fn(), contains: jest.fn() } },
  homeBtn: { classList: { add: jest.fn(), remove: jest.fn(), contains: jest.fn() } }
};
