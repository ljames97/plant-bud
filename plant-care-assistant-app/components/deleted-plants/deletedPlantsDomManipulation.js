import { createElement } from "../utils/globalDomManipulation"

const deletedPlantLogElements = () => {
  const archiveTitle = createElement({tagName: 'h1', textContent: 'Archived Plants', classEl: 'archive-title'});
}